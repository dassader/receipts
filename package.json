{
  "name": "receipt-pwa",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc && vite build && node scripts/copy-spa-fallback.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "docker:build": "scripts/docker-receipts.sh build",
    "docker:run": "scripts/docker-receipts.sh run",
    "docker:stop": "scripts/docker-receipts.sh stop",
    "docker:delete": "scripts/docker-receipts.sh delete"
  },
  "dependencies": {
    "lucide-preact": "^1.21.0",
    "preact": "^10.29.2",
    "preact-iso": "^2.12.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.5",
    "typescript": "^5.9.3",
    "vite": "^6.4.3"
  }
}
